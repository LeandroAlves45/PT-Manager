name: CI placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Explain placeholder
        run: echo "The real CI pipeline is implemented in the nexts sprints."
