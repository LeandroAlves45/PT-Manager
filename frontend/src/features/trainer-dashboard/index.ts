/**
 * API pública da feature trainer-dashboard.
 *
 * Só as query keys saem daqui: as outras features precisam delas para invalidar o painel
 * depois de uma escrita, e nada mais.
 */
export { trainerDashboardKeys } from '@/features/trainer-dashboard/api/dashboard';
