/**
 * API pública da feature clients.
 *
 * Nenhuma outra parte da aplicação importa ficheiros internos desta pasta: o que não
 * estiver aqui não é para ser usado de fora.
 */
export { clientKeys } from '@/features/clients/api/keys';
export { useClientSearchQuery } from '@/features/clients/api/queries';
