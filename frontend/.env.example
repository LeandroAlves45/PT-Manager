# Origem da API .NET em desenvolvimento (perfil "https" de launchSettings.json).
# Tem de ser HTTPS: o cookie de refresh usa o prefixo __Secure- e o CORS do backend
# recusa origens que não sejam HTTPS.
VITE_API_BASE_URL=https://localhost:7186
